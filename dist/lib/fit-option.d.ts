/**
 * Given an options object, returns the parameters required to
 * resize the image for best fit if applicable.
 */
import { CrushConfig } from './crush-config';
export declare function fit(options: CrushConfig): any;
export default fit;
